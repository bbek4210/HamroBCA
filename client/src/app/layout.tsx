import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "HamroBCA - BCA Resources Platform",
  description: "Complete resource platform for BCA students in Nepal. Access notes, past papers, assignments and more.",
  keywords: "BCA, Nepal, Notes, Past Papers, Computer Application, Education",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}